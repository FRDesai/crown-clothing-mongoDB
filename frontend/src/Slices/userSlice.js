import { createSlice } from "@reduxjs/toolkit";

export const User = createSlice({
  name: "user",
  initialState: {},
  reducers: {
    register: ()=>{

    },
    login: ()=>{
        
    }
  },
});
